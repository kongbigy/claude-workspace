# SECURITY.md — 키 노출 비상 매뉴얼

> "키 노출된 것 같아요" 라고 말하면 Claude Code가 이 문서 순서대로 안내합니다.
> 당황하지 말고 **1번부터 순서대로** 진행하세요.

---

## 🚨 0단계 — 침착하게, 순서대로

1. 지금 하던 작업을 멈추세요.
2. 어떤 키가 노출됐는지 먼저 특정하세요 (아래 "서비스별 대응" 참고).
3. 확실하지 않으면 **의심되는 모든 키를 다 폐기**하는 게 안전합니다.

---

## 1단계 — 노출 범위 확인

다음 중 어디에 키가 노출됐는지 확인:

- [ ] Git 커밋/푸시 (GitHub 등 원격 저장소에 올라감)
- [ ] 채팅/이메일/슬랙 등에 붙여넣음
- [ ] 스크린샷/화면 공유
- [ ] 코드에 하드코딩된 채로 배포됨
- [ ] 로그 파일에 출력됨

**Git에 이미 푸시됐다면:** 커밋 히스토리에서 지워도 이미 노출된 것으로 간주하고 반드시 재발급하세요. (`git filter-branch`나 히스토리 삭제만으로는 안전하지 않습니다.)

---

## 2단계 — 서비스별 대응

### 🔑 OpenRouter API 키 (`sk-or-v1-...`)

1. https://openrouter.ai/keys 접속 → 노출된 키 **즉시 삭제(Revoke)**
2. 새 키 발급
3. `~/claude-workspace/.env` 파일에서 `OPENROUTER_API_KEY` 값 교체
4. https://openrouter.ai/activity 에서 비정상적인 사용 이력(모르는 요청, 급증한 비용) 확인

### 🖥️ 오라클(Oracle) 서버 SSH 키 (`~/.ssh/oracle-server.key`)

1. Oracle Cloud 콘솔 로그인 → 해당 인스턴스의 **SSH 공개키 교체**
   - Compute → Instance → 콘솔 접속 후 `~/.ssh/authorized_keys`에서 기존 공개키 제거
2. 로컬에서 새 키 쌍 생성:
   ```
   ssh-keygen -t ed25519 -f ~/.ssh/oracle-server-new.key
   ```
3. 새 공개키를 서버의 `authorized_keys`에 등록 (콘솔 접속 이용, 기존 키로 접속 금지)
4. 기존 `oracle-server.key` 파일 삭제, SSH config의 `IdentityFile` 경로를 새 키로 변경
5. 서버 로그인 이력 확인: `last`, `/var/log/auth.log` (또는 `/var/log/secure`)에서 낯선 IP 접속 여부 확인

### 🌐 WordPress 관리자 계정 / 앱 비밀번호

1. WordPress 관리자 페이지 → **사용자 → 프로필 → 애플리케이션 비밀번호**에서 노출된 비밀번호 즉시 해지
2. 관리자 계정 비밀번호도 함께 변경 (같이 노출됐을 가능성)
3. 새 애플리케이션 비밀번호 발급 → `.env`의 관련 값 교체
4. **도구 → 사이트 상태 → 로그** 또는 보안 플러그인(예: Wordfence)에서 최근 로그인 이력, 낯선 관리자 활동 확인
5. 의심스러운 플러그인/테마 파일 변경이 있는지 확인

---

## 3단계 — 환경변수 교체

1. `~/claude-workspace/.env` 파일 열기 (Claude Code에게 절대 내용 공유 요청하지 말 것 — 직접 열어서 수정)
2. 노출된 키를 새로 발급받은 값으로 교체
3. 관련 서비스/스크립트 재시작 (있다면)
4. 교체 후 정상 동작 확인

---

## 4단계 — 재발 방지 체크

- [ ] `.gitignore`에 `.env`, `*.key`, `*.pem`, `id_rsa`, `credentials` 패턴이 포함되어 있는지 확인
- [ ] 이미 커밋된 적이 있다면 `git log -- .env` 등으로 과거 이력에도 없는지 확인
- [ ] 키를 다시 채팅창에 붙여넣지 않기 — 필요하면 마스킹된 형태로만 공유

---

## 연락처 / 참고 링크 메모

> 아래 빈칸은 필요할 때 직접 채워 넣으세요.

- OpenRouter 계정 이메일: [ ]
- Oracle Cloud 계정: [ ]
- WordPress 사이트 주소: [ ]

---

*이 매뉴얼은 CLAUDE.md의 "🚨 비상시 — 키 노출 의심" 절차와 연동됩니다.*
